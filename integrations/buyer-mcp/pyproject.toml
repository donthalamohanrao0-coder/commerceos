[project]
name = "commerceos-buyer-mcp"
version = "0.1.0"
description = "MCP server that lets an AI assistant transact with a CommerceOS merchant via the Agent Commerce API."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0,<2",
    "httpx>=0.27.0",
]

[tool.uv]
package = false
